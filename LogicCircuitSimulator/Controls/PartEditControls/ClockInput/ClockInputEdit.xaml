<UserControl
    x:Class="LogicCircuitSimulator.ClockInputEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LogicCircuitSimulator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
		
		<!-- Period label -->
		<TextBlock Style="{StaticResource WhiteTextBlockStyle}"
			FontSize="{StaticResource FontSizeBig}"
			Text="{Binding Header, Mode=OneTime}"
			Margin="0 0 0 10"/>

		<!-- TextBox with the period settings -->
		<TextBox Style="{StaticResource SideMenuTextBoxStyleUnder8}"
			Text="{Binding Period, Mode=TwoWay}"
			MaxLength="4"
			Grid.ColumnSpan="2"/>
			
	</StackPanel>
</UserControl>
